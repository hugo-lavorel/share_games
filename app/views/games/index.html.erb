<div class="container">
  <div class="row justify-content-center">
    <div class="col-6">
      <% @games.each do |game| %>
        <%= render 'shared/game-card', game: game %>
      <% end %>
    </div>
  </div>
</div>
